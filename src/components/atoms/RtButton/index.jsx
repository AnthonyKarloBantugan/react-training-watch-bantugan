import React from "react";
import { Button, ButtonGroup } from "@chakra-ui/react";

const RtButton = ({ children }) => {
	return <Button colorScheme="blue">{children}</Button>;
};

export default RtButton;
